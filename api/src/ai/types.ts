export interface KeyResult {
  title: string;
}

export interface Initiative {
  title: string;
  description: string;
  priority: string;
}

export interface WorkItem {
  title: string;
  description: string;
  priority: string;
  storyPoints: number;
}