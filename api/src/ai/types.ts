export interface KeyResultType {
  title: string;
}

export interface InitiativeType {
  title: string;
  description: string;
  priority: string;
}

export interface WorkItemType {
  title: string;
  description: string;
  priority: string;
  storyPoints: number;
}
